# Copy to .env.local (git-ignored).
#
# Optional: a GitHub token lifts the anonymous limit (60 requests an hour,
# 10 searches a minute) while you develop. A fine-grained token with no
# permissions is enough for public repositories. The dev server's proxy
# attaches it; it is never bundled into the app.
GITHUB_TOKEN=
